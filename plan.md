1. **Run tests**:
   - Use `run_in_bash_session` to run `./install.sh` and the `node -e` manifest validation script to ensure the changes are correct and have not introduced regressions.
2. **Pre-commit**:
   - Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
3. **Submit**:
   - Submit the branch once all changes are successfully integrated and verified using the `submit` tool with `branch_name`, `commit_message`, `title`, and `description`.
